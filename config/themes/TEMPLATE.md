# 🎨 How to Create Your Own Theme

This is a template for creating custom themes in NCMDS (No Code Markdown Sites).

## Instructions

1. Copy this file
2. Rename it (e.g., `my-theme.yaml`)
3. Customize the colors
4. Save it in `config/themes/`
5. Update `config/config.yaml` with `theme_name: "my-theme"`
